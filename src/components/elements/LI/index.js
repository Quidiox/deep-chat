import styled from 'styled-components/macro'

const LI = styled.li`
  padding: 1px;
  list-style: none;
  border-bottom: 1px solid #fff;
  &:last-of-type {
    border-bottom: none;
  }
`

export default LI
